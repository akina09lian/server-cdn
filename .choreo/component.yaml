schemaVersion: "1.2"
type: service

metadata:
  name: docker-hello-world
  displayName: Docker Hello World
  description: Simple Docker based service

build:
  dockerfile: Dockerfile
  context: .

run:
  port: 8080
